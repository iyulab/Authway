# ============================================================
# Authway 배포 스크립트용 환경변수
# ============================================================
# 이 파일을 .env로 복사하고 실제 토큰 값을 입력하세요.
# .env 파일은 .gitignore에 포함되어 있어 안전합니다.
# ============================================================

# Admin Dashboard 배포 토큰
# Azure Portal → Static Web Apps → authway-admin → Manage deployment token
ADMIN_DEPLOYMENT_TOKEN=your-admin-deployment-token-here

# Login UI 배포 토큰
# Azure Portal → Static Web Apps → authway-login → Manage deployment token
LOGIN_DEPLOYMENT_TOKEN=your-login-deployment-token-here
