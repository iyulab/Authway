name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go:
    name: Go build / vet / test
    runs-on: ubuntu-latest

    # The migration and client-persistence tests need a real Postgres: the
    # constraints they guard (`redirect_uris text[] NOT NULL`, CHECK constraints,
    # array types) cannot be expressed by the SQLite harness the other tests use,
    # and a defect already escaped through that blind spot (commit 7082776).
    # They were written gated on MIGRATE_SMOKE_DSN and, until this service
    # existed, silently SKIPPED here — automated coverage was zero.
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: authway
          POSTGRES_USER: authway
          POSTGRES_PASSWORD: authway
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U authway -d authway"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache: true

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test (central API)
        # Each migration test creates and drops its own throwaway database on
        # this server, so the DSN's own database is only used by the client
        # persistence tests, which bring the schema up via the real migrator.
        env:
          MIGRATE_SMOKE_DSN: postgres://authway:authway@localhost:5432/authway?sslmode=disable
        run: go test ./apps/central/api/...

      # Fails the build if the Postgres-gated tests silently stop running — a
      # skip here looks identical to a pass in the summary above.
      - name: Verify Postgres-gated tests actually ran
        env:
          MIGRATE_SMOKE_DSN: postgres://authway:authway@localhost:5432/authway?sslmode=disable
        run: |
          set -euo pipefail
          out="$(go test -v -count=1 \
            -run 'TestMigrateFreshDatabase|TestInitialSchemaIsIdempotentOnPopulatedDatabase|TestCreateClient_MachineToMachine_PersistsEmptyRedirectURIs' \
            ./apps/central/api/internal/database/ ./apps/central/api/pkg/client/)"
          echo "$out"
          for name in TestMigrateFreshDatabase \
                      TestInitialSchemaIsIdempotentOnPopulatedDatabase \
                      TestCreateClient_MachineToMachine_PersistsEmptyRedirectURIs; do
            if ! grep -q "^--- PASS: $name" <<<"$out"; then
              echo "::error::$name did not run against Postgres (skipped or failed)"
              exit 1
            fi
          done

  # auth-api is its own Go module (no go.work tying it to the root module), so
  # the `go` job above — which builds/vets/tests from the repo root — never
  # touches it. It owns login/consent/logout/OAuth/social-login: exactly the
  # kind of auth-flow surface where an untested regression is most costly.
  auth-api:
    name: auth-api build / vet / test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/branding/auth-api
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache: true
          cache-dependency-path: apps/branding/auth-api/go.sum

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./...

  # The two Vite apps live outside the pnpm workspace (each has its own
  # package-lock.json), so they were invisible to CI — their test suites only ever
  # ran on a developer machine. Build + test them here; no artifacts are uploaded
  # (CI verifies, the release pipeline publishes).
  apps:
    name: UI apps build / test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app:
          - apps/central/admin
          - apps/branding/auth-ui
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ${{ matrix.app }}/package-lock.json

      - name: Install dependencies
        working-directory: ${{ matrix.app }}
        run: npm ci

      - name: Build (tsc + vite)
        working-directory: ${{ matrix.app }}
        run: npm run build

      - name: Test
        working-directory: ${{ matrix.app }}
        run: npx vitest run

  packages:
    name: TS packages build / typecheck / tarball verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v6
        with:
          version: 10.32.1
          run_install: false

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        # --config.strict-dep-builds=false: pnpm 10은 전이 postinstall (esbuild/sharp 등)을
        # 기본 strict 차단한다. 빌드 자체에는 tsup 내장 esbuild를 쓰므로 postinstall
        # 바이너리는 CI에서 꼭 필요하지 않다.
        run: pnpm install --frozen-lockfile --config.strict-dep-builds=false

      - name: Build @authway/client
        run: pnpm --filter @authway/client build

      - name: Build @authway/react
        run: pnpm --filter @authway/react build

      - name: Typecheck @authway/client
        run: pnpm --filter @authway/client typecheck

      - name: Typecheck @authway/react
        run: pnpm --filter @authway/react typecheck

      # Guards against regression to `npm publish`, which leaves `workspace:*`
      # unresolved in the tarball and breaks every downstream `npm install`.
      # See claudedocs/issues/closed/ISSUE-Authway-20260413-pnpm-publish-workspace-protocol.md
      - name: Verify tarballs contain no `workspace:` protocol
        run: |
          set -euo pipefail
          for pkg in client react; do
            pack_dir="$(mktemp -d)"
            extract_dir="$(mktemp -d)"
            (cd "packages/$pkg" && pnpm pack --pack-destination "$pack_dir" >/dev/null)
            tarball="$(ls "$pack_dir"/*.tgz | head -n1)"
            tar -xzf "$tarball" -C "$extract_dir"
            if grep -q '"workspace:' "$extract_dir/package/package.json"; then
              echo "::error::@authway/$pkg tarball contains unresolved 'workspace:' protocol"
              exit 1
            fi
            echo "✓ @authway/$pkg tarball verified"
          done
