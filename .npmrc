# pnpm-workspace.yaml의 `onlyBuiltDependencies`가 esbuild/sharp postinstall을
# 명시적으로 허용하고 있다. 그럼에도 CI 환경의 pnpm 10.32가 특정 조건
# (root package.json 부재로 추정)에서 whitelist를 해석하지 못하고
# ERR_PNPM_IGNORED_BUILDS로 실패하는 경우가 있어, 보조 안전장치로
# 동일 의도를 플래그로도 표현한다.
strict-dep-builds=false
